# Run `pre-commit install` once per clone, then hooks fire on every commit.
# Bypass with `pre-commit run --all-files` to scan everything.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
